
put statements in parentheses or lists where possible, link them with commas!
you can separate some statements with semicolons
make lists with dictionaries instead!
you can use `globals()` to store variables in a dict
use lambdas!
use ternaries instead of if statements!